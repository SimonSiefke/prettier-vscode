{
  "name": "extension-test",
  "version": "1.0.0-dev",
  "scripts": {
    "dev": "tsc -b -w",
    "build": "tsc -b",
    "e2e": "rimraf dist tsconfig.tsbuildinfo && npm run build && node dist/extensionTestMain"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.1",
    "@types/mocha": "^7.0.2",
    "@types/node": "^14.0.11",
    "@types/vscode": "^1.45.1",
    "fs-extra": "^9.0.1",
    "mocha": "^7.2.0",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.5",
    "vscode-test": "^1.4.0"
  }
}
