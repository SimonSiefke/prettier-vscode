{
  "name": "extension-test",
  "version": "1.0.0-dev",
  "scripts": {
    "dev": "tsc -b -w",
    "build": "tsc -b",
    "e2e": "rimraf dist tsconfig.tsbuildinfo && npm run build && node dist/extensionTestMain"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.3",
    "@types/mocha": "^8.0.3",
    "@types/node": "^14.14.7",
    "@types/vscode": "^1.51.0",
    "fs-extra": "^9.0.1",
    "mocha": "^8.2.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.5",
    "vscode-test": "^1.4.1"
  }
}
